# CD-Linter-with-yml
Requirements
Python 3.8 or higher
PyYAML

Install the dependency:
1.pip install pyyaml
2.pip install -r requirements.txt

Add yml file inside test_files directory
add file path in run_test.py file

RUN:
python run_test.py
